import DiscoverRecommendations from '@app/components/Discover/DiscoverRecommendations';
import type { NextPage } from 'next';

const RecommendationsPage: NextPage = () => {
  return <DiscoverRecommendations />;
};

export default RecommendationsPage;
